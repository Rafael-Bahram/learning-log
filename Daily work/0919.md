## JavaScript.info 2.1-2.8 ｜2026-09-19

### 2.1 Hello World

1. console.log("message") in a JS document can print a output--message ,with inputting node "name of the document" in the terminal of VScode ( by pressing Ctrl+` to open)
2. By pressing F12 in any web page and choose the Console panel ,and then input the code as alert("message") , it will also work ,but in a pop-up windows.

### .2 Code Structure

1. A semicolon (;) will be  omitted in most cases when a line break exists.
2. One-line comments start with two forward slash characters // ; and multiline comments start with a forward slash and an asterisk /* and end with an asterisk and a forward slash */. 
> 1.  Which is , //message is a comment and can be put into any place of a script,which doesn’t affect its execution because the engine simply ignores them.And /* message */ can include a multiline comment.
> 2. The hotkey for a single-line comment is Ctrl+/ , and the multilines comments' is Ctrl+Shift+/.
> 3. There may not be any /*...*/ inside another /*...*/ .

### 2.3 Use Strict

1. 'use strict' or "use strict" can be put at the start of most kinds of functions instead of the whole script. Doing that enables strict mode in that function only. But usually, people use it for the whole script.

### 2.4 Variables

1. let message to define a variable named message ,and input message = 'content' , then alert(message) , the out put will be content .
> In short , let message = content , the content can be accessed byt using the variable name .
> 1. When the value is changed, the old data is removed from the variable .
> 2. Two variables declared , the data can be copied from one to another .
> 3. One variable can only be daclared one time .
2. THE LIMITATIONS IN VARIABLES NAMES :
>1. The name must contain only letters, digits, or the symbols $ and _; 
> 2. The first character must not be a digit.
> 3. Case matters : apple and APPLe are not the same .
> 4. There is a list of reserved words rf , which cannot be used as variable names because they are used by the language itself. For example: let , class, return, and function are reserved.
3. const message to define a constant named message ,and input message = 'content' , then alert(message) , the out put will be content , and it will never be changed.
> constants are usually named using capital letters and underscores.